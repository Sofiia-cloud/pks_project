#include "HTMLGenerator.h"
#include <iostream>

void HTMLGenerator::generate(const ProjectData& data, const std::string& outputPath) {
    std::cout << "Generating HTML documentation to: " << outputPath << std::endl;
}